# EcommerceApp

once installed it or clone it then 
    1. run npm install 
    2. npm start
    3. install the expo go app from the play store.
    4. open expo go and scan the  QR code.
now you are ready to explore the app.